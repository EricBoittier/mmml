# Suggested resume config (generated by mmml md-system)
# prior job: md_out exit=2
include:
  - presets/heat-dt0.25-scale.yaml
  - presets/dynamics-flyoff-strict.yaml

defaults:
  backend: 'pycharmm'
  no_echeck_heat: true
  output_dir: 'scratch/md_out'

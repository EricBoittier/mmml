# `mmml umbrella-mbar`

MBAR post-processing for umbrella-sample runs.


## Usage

```bash
mmml umbrella-mbar --help
```

!!! note
    This command defines `build_parser()` but help could not be loaded (optional deps missing in the doc build environment). Run `mmml umbrella-mbar --help` locally for flags.



---

[← CLI overview](../index.md) · [All commands](../index.md#command-index)

# Run with: uv run --with snakemake --with snakemake-executor-plugin-slurm snakemake --profile profiles/slurm
executor: slurm
jobs: 15
latency-wait: 120
retries: 0
default-resources:
  - gpu=1
  - runtime=30
  - mem_mb=8000
  - cpus_per_task=4
  - slurm_partition=gpu
  - slurm_gres=gpu:1
